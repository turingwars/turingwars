import styled from 'styled-components';
import { CRT_GLITCH_LG, COLOR_PRIMARY } from '../../style';

export const ScreenTitle = styled.h1`
    ${CRT_GLITCH_LG}
    color: ${COLOR_PRIMARY}
    font-size: 40px;
`;