import { GRAY } from 'frontend/style';
import { Row } from '../layout/Row';

export const EditorActions = Row.extend`
    justify-content: flex-start;
    font-size: 20px;
    border-bottom: 1px ${GRAY} solid;
`;
